import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color white = Color(0xFFFFFFFF);
  static const Color offWhite = Color(0xFFFAF9F6);     // Soft Ivory per spec
  static const Color cream = Color(0xFFF2EDE8);

  // Primary Accents
  static const Color camel = Color(0xFFC19A6B);        // Soft Camel per spec
  static const Color camelLight = Color(0xFFF5EDE0);   // pale tint for chips/bg
  static const Color rose = Color(0xFFD4856A);         // muted terracotta/rose
  static const Color sage = Color(0xFF7A9E87);         // muted sage green

  // Text & UI Elements
  static const Color charcoal = Color(0xFF1A1A1A);     // Dark Ink per spec
  static const Color ink = Color(0xFF3A3A3C);          // secondary text
  static const Color grey = Color(0xFF8E8E93);         // muted labels
  static const Color greyLight = Color(0xFFDDD9D4);    // borders / dividers
  static const Color greySubtle = Color(0xFFEFECE8);   // subtle bg tint

  // Status Colors
  static const Color success = Color(0xFF4A7C59);      // muted forest green
  static const Color error = Color(0xFFC0392B);        // deep red
  static const Color warning = Color(0xFFD4956A);      // warm amber
}
